# 웹

- 브라우저는 요청기 및 프린터
- 브라우저는 클라이언트와 서버로 나뉘어져 있음.
- url은 uniform resource locator ⇒ 브라우저는 요청기인데 저 주소(url)로 요청을 보낸다. response and request cycle

터미널은 운영체제 측면에서 컴퓨터를 제어하는 곳

콘솔은 각자의 언어를 간단하게 돌려볼 수 있는 실행 환경

(편하지만 날라가)

주피터노트는 콘솔과 파이썬파일을 적절히 섞음

숫자 (라우팅) →매핑 인간이 영어로 쓰기에 편하게 바꿔줌(도메인)

효율적으로 쓰고싶어서 구조화 

그 구조화 된 것을 접근하는걸 다음주에 배움

# API

요청기가 주소(api)로 쏘면 데이터를 받음

API:application programming interface

API 는 프로그래밍 하는데 있어서 편한 존재